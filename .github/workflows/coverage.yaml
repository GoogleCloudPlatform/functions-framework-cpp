name: C++ Conformance CI

on:
  push:
    branches: [ main ]
  pull_request:

env:
  vcpkg_SHA: "5214a247018b3bf2d793cea188ea2f2c150daddd"

jobs:
  coverage:
    name: coverage
    runs-on: ubuntu-20.04
    steps:
    - name: install tools
      run: sudo apt install ninja-build lcov
    - uses: actions/checkout@v2
    - name: clone-vcpkg
      working-directory: "${{runner.temp}}"
      run: >
        mkdir -p vcpkg &&
        curl -sSL "https://github.com/microsoft/vcpkg/archive/${{env.vcpkg_SHA}}.tar.gz" |
        tar -C vcpkg --strip-components=1 -zxf -
    - name: cache-vcpkg
      id: cache-vcpkg
      uses: actions/cache@v2
      with:
        # Preserve the vcpkg binary *and* the vcpkg binary cache in the build cache
        path: |
          ~/.cache/vcpkg
          ~/.cache/bin
        key: |
          vcpkg-${{ env.vcpkg_SHA }}-build-codecov-3-${{ hashFiles('vcpkg.json') }}
        restore-keys: |
          vcpkg-${{ env.vcpkg_SHA }}-build-codecov-3-
    - name: boostrap-vcpkg
      run: ci/restore-vcpkg-from-cache.sh "${{runner.temp}}/vcpkg"

    - name: configure
      run: >
        cmake -S . -B ${{runner.temp}}/build -GNinja
        -DCMAKE_EXPORT_COMPILE_COMMANDS=ON
        -DCMAKE_BUILD_TYPE=Debug
        -DCMAKE_CXX_FLAGS=--coverage
        -DCMAKE_CC_FLAGS=--coverage
        -DCMAKE_TOOLCHAIN_FILE="${{runner.temp}}/vcpkg/scripts/buildsystems/vcpkg.cmake"
    - name: build
      run: cmake --build ${{runner.temp}}/build
    - name: test
      working-directory: ${{runner.temp}}/build
      run: ctest --output-on-failure --timeout=60s

    - name: coverage-debug
      run: |
        find "${{runner.temp}}/build" -name '*.gcda'
        find "${{runner.temp}}/build" -name '*.gcno'
        find "${{runner.temp}}/build" -name '*.gcov'
        find "${{runner.temp}}/build" -name '*.o'
    - name: coverage-upload
      run: >
        /bin/bash <(curl -s https://codecov.io/bash) -X coveragepy -x /usr/bin/gcov \
            -s "${{runner.temp}}/build"

    - name: Setup Go
      uses: actions/setup-go@v2
      with:
        go-version: '1.13'

    - name: Run Cloud Event conformance tests
      uses: GoogleCloudPlatform/functions-framework-conformance/action@v0.3.2
      with:
        functionType: 'cloudevent'
        useBuildpacks: false
        validateMapping: false
        cmd: '${{runner.temp}}/build/google/cloud/functions/integration_tests/cloud_event_conformance'

    - name: Run HTTP conformance tests
      uses: GoogleCloudPlatform/functions-framework-conformance/action@v0.3.2
      with:
        functionType: 'http'
        useBuildpacks: false
        validateMapping: false
        cmd: '${{runner.temp}}/build/google/cloud/functions/integration_tests/http_conformance'

